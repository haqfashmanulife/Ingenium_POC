<changeSet id="zap-trt-status" author="firoz">

    <!-- BEFORE CHECK -->
    <preConditions onFail="HALT">
        <sqlCheck expectedResult="5">
            SELECT COUNT(*) FROM TLQBASE WHERE STATUS_CD = 'A'
        </sqlCheck>
    </preConditions>

    <!-- UPDATE -->
    <sql>
        UPDATE TLQBASE SET STATUS_CD = 'I' WHERE STATUS_CD = 'A'
    </sql>

    <!-- AFTER CHECK -->
    <preConditions onFail="HALT">
        <sqlCheck expectedResult="0">
            SELECT COUNT(*) FROM TLQBASE WHERE STATUS_CD = 'A'
        </sqlCheck>
    </preConditions>

</changeSet>